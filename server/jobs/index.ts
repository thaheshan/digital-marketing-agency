// Jobs registered here — implementations coming soon
export const initializeJobs = () => {
  console.log("[Jobs] Job scheduler initialized");
  // TODO: add emailDrip, scoreRecalculate, socialFetch jobs
};
